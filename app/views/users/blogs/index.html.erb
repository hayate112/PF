<h2>ブログ一覧</h2>

<% @blogs.each do |blog| %>
  <%= link_to blog_path(blog.id) do %>
    <div>
      <div><%= blog.title %></div>
      <div><%= blog.body %></div>
      <div><%= blog.genre.name %></div>
    </div>
  <% end %>
<% end %>