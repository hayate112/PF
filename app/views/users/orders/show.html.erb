<h2>注文履歴詳細</h2>

<table>
  <tr>
    <th>注文日時</th>
    <th>配送先</th>
    <th>支払方法</th>
    <th>ステータス</th>
  </tr>
  <tr>
    <td><%= @order.created_at.strftime('%Y/%m/%d %H:%M:%S') %></td>
    <td>
      〒<%= @order.postal_code %><br>
      <%= @order.prefectures %><%= @order.city %><br>
      <%= @order.name %>
    </td>
    <td>
      <% if @order.payment == 0 %>
        クレジットカード
      <% else %>
        銀行振込
      <% end %>
    </td>
    <td>
        <% if @order.shipping_status == 0 %>
          入金待ち
        <% elsif @order.shipping_status == 1 %>
          発送準備中
        <% elsif @order.shipping_status == 2 %>
          発送完了
        <% else %>
          お届け済み
        <% end %>
      </td>
  </tr>
</table>

<table>
  <tr>
    <th>商品合計</th>
    <th>配送料</th>
    <th>ご請求金額</th>
  </tr>
  <tr>
    <td><%= @order.amount - @order.shipping_fee %></td>
    <td><%= @order.shipping_fee %></td>
    <td><%= @order.amount %></td>
  </tr>
</table>

<table>
  <tr>
    <th>商品名</th>
    <th></th>
    <th>単価（税込）</th>
    <th>個数</th>
    <th>小計</th>
    <th>商品評価</th>
  </tr>
  <% @order.order_items.each do |order_item| %>
    <tr>
      <td></td>
      <td><%= order_item.item.name %></td>
      <td><%= (order_item.item.price * 1.08).round %></td>
      <td><%= order_item.count %></td>
      <td><%= ((order_item.item.price * order_item.count) * 1.08).round %></td>
      <td><%= link_to '評価', review_item_path %></td>
    </tr>
  <% end %>
</table>